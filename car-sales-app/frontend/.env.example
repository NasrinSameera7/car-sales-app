# Leave blank for local dev (Vite proxy handles it — see vite.config.js).
# In production, set this to your deployed backend's URL, e.g.
# VITE_API_URL="https://car-sales-api.onrender.com"
VITE_API_URL=
